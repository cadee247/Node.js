<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Subscribers</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/bootstrap.css"> 
</head>
<body>
    <div class="container">
        <h1 class="text-center">Subscribers</h1>
        <ul class="list-group">
            <% subscribers.forEach(subscriber => { %>
                <li class="list-group-item">
                    <%= subscriber.name %> - <%= subscriber.email %>
                    <a href="/subscribers/<%= subscriber._id %>/edit" class="btn btn-primary btn-sm">Edit</a>
                    <form action="/subscribers/<%= subscriber._id %>?_method=DELETE" method="POST" style="display:inline;">
                        <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                    </form>
                </li>
            <% }) %>
        </ul>
        <a href="/subscribers/new" class="btn btn-success mt-3">Add New Subscriber</a>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>